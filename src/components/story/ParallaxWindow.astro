---
import { Image } from "astro:assets";
import type { ParallaxWindowArgs } from "./util/types/types";
// import type { DefaultObjectPromise } from "./util/types/types";
// import type { Loader } from "./util/types/types";
// import {isImageLoader} from "./util/typeCheck";
// import { getEntry } from "astro:content";

const {
  sectionId, image
  // sectionId,
  // image,
  // x,
  // y,
  // windowWidth = "initial",
  // windowHeight,
  // imageWidth = "auto",
  // imageHeight,
} = Astro.props as ParallaxWindowArgs; //ImageWindowArgs;

// Parse windowWidth, windowHeight, imageWidth, imageHeight?
// Set element id


// image === null ? "" : typeof image === 'function' ? image() : image

// const test: Loader<DefaultObjectPromise<ImageMetadata>> = () => new Promise((resolve, reject)=>{
//   const data = {src: "", format: "jpg", height: 200, width: 200} as ImageMetadata;
//   return {default: data};
// });

// const imageLoader = ((typeof image === 'string')
//   ? await (async () => {
//     const g = await getEntry('backdropImages', image);
//     if(!g) throw `No backdrop images found matching "${image}"`;
//     const entry = g?.data;
//     return entry as Loader<DefaultObjectPromise<ImageMetadata>>;
//   })()
//   : image
// );

if(typeof image === 'string') {
  throw new Error("Image specification as string is not yet implemented");
}


// console.log('imageLoader:', image, sectionId);

---

<div class="parallax-image-wrapper" data-section-id={sectionId}>
  {
    (image === null ?
      <div class="parallax-image-null parallax-backdrop-item"></div>
      : (<Image class="parallax-image parallax-backdrop-item" src={image()} alt={`Backdrop image for section "${sectionId}"`}/>)
    )
  }
</div>

<!--
<picture class="image_cont">
   <source srcset="https://images.unsplash.com/photo-1506744038136-46273834b3fb?crop=entropy&amp;cs=srgb&amp;fm=jpg&amp;ixid=MnwxNDU4OXwwfDF8cmFuZG9tfHx8fHx8fHx8MTY0NDMzNTU3OA&amp;ixlib=rb-1.2.1&amp;q=85&amp;w=1500" media="(min-width: 1500px)">
   <source srcset="https://images.unsplash.com/photo-1506744038136-46273834b3fb?crop=entropy&amp;cs=srgb&amp;fm=jpg&amp;ixid=MnwxNDU4OXwwfDF8cmFuZG9tfHx8fHx8fHx8MTY0NDMzNTU3OA&amp;ixlib=rb-1.2.1&amp;q=85&amp;w=1000" media="(min-width: 700px)">
   <img class="" src="https://images.unsplash.com/photo-1506744038136-46273834b3fb?crop=entropy&amp;cs=srgb&amp;fm=jpg&amp;ixid=MnwxNDU4OXwwfDF8cmFuZG9tfHx8fHx8fHx8MTY0NDMzNTU3OA&amp;ixlib=rb-1.2.1&amp;q=85&amp;w=600" data-speed="auto" data-lag="0" style="translate: none; rotate: none; scale: none; transform: translate(0px, 0px); will-change: transform;">
  </picture>
-->

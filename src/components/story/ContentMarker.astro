---

/**
 * Wrapper for ContentMarker.tsx Preact component
 */

import PreactContentMarker from "./ContentMarkerPreact";

import type {ContentMarkerProps} from "./ContentMarkerPreact";

// eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
const props: ContentMarkerProps = Astro.props;

---

<PreactContentMarker {...props} client:load>
    <slot/>
</PreactContentMarker>

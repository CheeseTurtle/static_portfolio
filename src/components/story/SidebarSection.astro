---
import type { MarkdownInstance } from "astro";
import type { StorySectionFrontmatter } from "./util/types/types";

// const {sectionId} = Astro.props;

const {frontmatter, ...data} = Astro.props as MarkdownInstance<StorySectionFrontmatter>;


---

<div class="sidebar-section" data-section-id={frontmatter.id}>
    

</div>